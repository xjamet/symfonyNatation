<!DOCTYPE html>
<html lang="en">
<head>
    {% block stylesheets %}

        <link rel="stylesheet" href="{{ asset('css/monCss.css') }}">
        <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
        <script src="{{ asset('js/jquery.min.js') }}"></script>
        <script src="{{ asset('js/bootstrap.js') }}"></script>


    {% endblock %}

    {% block sidebar %}
        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    {% if is_granted('ROLE_USER') %}
                    <a class="navbar-brand" href="accueilAdmin">iNatation</a>
                    {% elseif is_granted('ROLE_USER') %}
                    <a class="navbar-brand" href="#">iNatation</a>
                    {% endif %}
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        {% if is_granted('ROLE_USER') %}

                        <li class="active"><a href="#">Concours <span class="sr-only">(current)</span></a></li>
                        <li><a href="#">Services</a></li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">creation Rôle <span class="caret"></span></a>
                            {% if is_granted('ROLE_SUPER_ADMIN') %}
                            <ul class="dropdown-menu">
                                <li><a href="creationJuge">Juges</a></li>

                                <li><a href="creationOrganisateur">organisateurs concours</a></li>

                            </ul>
                            {% endif %}
                            {% if is_granted('ROLE_JUGE') %}
                            {% endif %}
                        </li>
                        {% if is_granted('ROLE_SUPER_ADMIN') %}
                        <li><a href="administrateur">Administrateur</a></li>
                        {% endif %}
                        {% if is_granted('ROLE_ORGANISATEUR') %}
                            <li><a href="competition">Competition</a></li>

                        {% endif %}
                        <li><a href="{{ path('fos_user_security_logout') }}">logout</a></li>


                            {% endif %}
                        <li><a href="{{ path('fos_user_security_login') }}">login</a></li>

                    </ul>
                    <form class="navbar-form navbar-left">
                        <div class="form-group">
                            <input type="text" class="form-control" placeholder="Search">
                        </div>
                        <button type="submit" class="btn btn-default">Submit</button>
                    </form>

                </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
        </nav>
    {% endblock %}
</head>
<body>
{% block body %}{% endblock %}
</body>
</html>